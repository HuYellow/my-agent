# UIUX 重新设计：青橙浅色主题 & 黑金深色主题 (修订版)

保持现有前端布局结构 (HTML/JSX) 完全不变，仅修改 `styles.css` 中的设计令牌和组件样式。

---

## User Review Required

> [!IMPORTANT]
> 本次修改**仅涉及 CSS**，不修改任何 `.tsx` 文件中的布局结构、组件层级或类名。所有视觉变化通过 CSS 自定义属性和选择器实现。

> [!WARNING]
> 本修订版调整了**区域的视觉隔离**：
> 顶部 `toolbar` 和左侧 `sidebar` 将被设计为统一色调的“实体框架”（采用纯净磨砂或单色弱噪点），所有的发光效果（光晕渐变）将**仅局限在右侧核心工作区 (`.app-container` 内的 `.main-content` 定位层)**，加强区域功能的区分，减少视觉疲劳。

---

## 一、设计理念

### 浅色主题：暖橙为主，青芒为辅 (Orange-Cyan)

| 维度 | 设计方向 |
|------|----------|
| **主色调** | 暖橙色 `#EA580C` — 活力、创意、行为召唤 (用于主要按钮、高亮项、核心边框) |
| **辅助色** | 青色/青绿色 `#0891B2` — 清新、专业 (用于辅助标签、代码块提示、Agent消息) |
| **工作区背景** | 奶油白/沙色 `#F5F0EB` — 带有暖橙/青色的柔和光晕渐变发光 (`page-glow`) |
| **框架基调 (Toolbar/Sidebar)** | 干净的中型浅米色或云白色磨砂底色，**取消发光装饰和剧烈的渐变**，使其显得平整统一，作为干净的"画框"。 |

### 深色主题：极限纯黑与奢华倒金 (Black-Gold)

| 维度 | 设计方向 |
|------|----------|
| **主色调** | 奢华金 `#D4A853` / 亮金 `#F5C563` — 尊贵、精确 (核心层操作) |
| **辅助色** | 琥珀金 `#F59E0B` — 次级强调与警示 |
| **工作区背景** | 极限纯黑 `#000000` 到 `#09090B` — 提供最极致的对比度，配合中央画布微弱的金色光晕。 |
| **框架基调 (Toolbar/Sidebar)** | 极致深黑/炭黑磨砂材质，**无多余光效**，与主画板形成极简克制的隔离，凸显中部业务。 |

---

## 二、设计规格调整细节

### 1. 变量层次剥离
之前 `body::before` 和 `body::after` 控制着全局的光晕，导致特效漫灌至 Sidebar 与 Toolbar 底部。
**修改方案**：
取消 `body` 级的全局光晕。将光晕变量（`--page-glow-a`, `--page-glow-b` 等）的应用范围移到专属的工作区画板容器上（例如 `.main-content::before` 或类似背景层），从而**确保侧边栏和顶栏处于发光层之外**。

### 2. Sidebar & Toolbar 色彩统合
* **浅色主题**：
  Sidebar & Toolbar 使用平整的 `rgba(250, 246, 241, 0.95)` 带有细微的模糊毛玻璃，移除原有的叠加渐变和彩色光晕。文字颜色使用沉稳深灰，活跃项目高亮采用克制的单色色块。
* **深色主题**：
  Sidebar & Toolbar 使用死寂的纯黑磨砂 `rgba(9, 9, 11, 0.95)`。原先的彩色高光投影将被取消，仅保留纤细的主题色（金色）左侧/底侧边界线进行区域划分。

### 3. 主操作点强化
* **Send / Submit 按钮**：
  浅色主题使用醒目的暖橙到深橙垂直渐变 (`#EA580C` -> `#C2410C`)。
  深色主题使用奢华香槟金垂直渐变 (`#D4A853` -> `#8B6914`)。
* **Skill Logo**：
  跟随上述按钮的渐变色块，匹配核心色彩。

---

## 三、CSS 改造执行计划 (Proposed Changes)

### [MODIFY] [styles.css](file:///c:/Users/huyel/my-agent/apps/desktop/src/renderer/styles.css)

#### 1. 全局与主题令牌刷新
* 确认 `:root` (Light) 下 `accent` 为 `橙色`, `accent-alt` 为 `青色`。
* 确认 `html[data-theme="dark"]` (Dark) 下 `page-bg` 向纯黑 (`#000000` / `#09090b`) 靠拢，`accent` 为各类纯正 `金色`。

#### 2. 背景发光层迁移 (重要解耦)
* **移除**旧有的 `body::before` 的光晕代码。
* **转移**光晕和背景网格到 `.app-container` 中的只属于主视图的容器（如赋予 `.main-content` 绝对定位伪元素）。
* 确保 `.sidebar` 和 `.app-toolbar` 的背景没有渐变。例如：将 `--sidebar-bg` 和 `.sidebar::after` 里的 `radial-gradient` 全部清理，设为平整半透明背景色，依赖 `backdrop-filter: blur()`。

#### 3. 边框与边缘逻辑
* 统一设定 `--sidebar-border` 为淡灰色/淡透明，而不是带有主色调的渲染，以免影响整体框架的静谧感。
* Toolbar 底部边界线做锐利化处理，划清与渐变工作区的界限。

---

## Open Questions

无。当前的四个核心点（橙主青次、深色纯黑、渐变发送球、光效仅限工作区）逻辑自恰，明确要求了"外框克制，画板跳跃"的经典桌面端分层思路。

---

## Verification Plan

### Automated Tests
- 开发服务器检查 (`npm run dev`)。
- 使用 browser subagent 抓取 App 的带外框视窗。

### Manual Verification
- 检查顶栏 (Toolbar) 与左边栏 (Sidebar) 是否为干净、平整的纯色/深色区域。
- 只有工作区画板背景能看到网格与冷暖交叉的光晕渐变。
- 发送按钮的颜色是否与所选主题一致（橙色渐变/金色渐变）。
